version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:v1JuQjXhW6g23DdR7TVpFz4z0aanP5uGGzXf2r0GPphCa71fF7qoIj6z197rjR34@46.62.171.65:5432/dijitalkartvizit?sslmode=disable
      - NEXTAUTH_URL=https://fowksko404s008ogkg4gw0gk.46.62.171.65.sslip.io
      - NEXTAUTH_SECRET=your_secret_key_here
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  app-network:
    driver: bridge